sudo: required

language: generic

services:
  - docker

before_install:
  - echo "start!!"
  - docker build -t react/dev -f Dockerfile.dev .

script:
  - docker run -e CI=true react/dev npm test --coverage

after_success:
  - echo "Test Success"